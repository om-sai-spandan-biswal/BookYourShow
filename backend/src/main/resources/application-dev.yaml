#dev version of configuration  and Multipart file setting
server:
  port: 8081
  servlet:
    context-path: /api/v1

spring:
  datasource:
#    url: ${DB_URL}
      url: jdbc:postgresql://127.0.0.1:5432/test
      username: postgres
      password: Postgresql@Om123
  jpa:
    hibernate:
      ddl-auto: update
      dialect: {org.hibernate.dialect.PostgreSQLDialect}
    show-sql: true